apiVersion: v1
kind: LimitRange
metadata:
  name: rl-limits
  namespace: rl
spec:
  limits:
  - default: # this section defines default limits
      cpu: 200m
      memory: 256Mi
    defaultRequest: # this section defines default requests
      cpu: 100m
      memory: 128Mi
    type: Container
